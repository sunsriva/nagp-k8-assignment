apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: nagp-k8s
  labels:
    app: nagp-assignment
data:
  # Database connection config — consumed by the API service as env vars
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "productdb"
  APP_PORT: "3000"
